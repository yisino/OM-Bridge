# 统一 LF：本工程会被部署到 Linux（deploy/install.sh 直接在服务器上跑），
# CRLF 的 shell 脚本在 bash 下会因 shebang 带 \r 直接报错。
# 仓库内一律 LF，检出时也不转 CRLF（Windows 上的编辑器/git 均能正常处理 LF）。
* text=auto eol=lf

# 二进制类
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.mp4 binary
