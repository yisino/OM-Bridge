"""ComfyUI 上的方案集合。

一个方案 = 一个子包，内置至少一个 :class:`~om_bridge.core.solution.Solution` 子类。

目录约定（registry 依赖它，不要改）
-----------------------------------
* 方案放在 ``solutions/<方案名>/``；
* ``<方案名>`` 建议与 :attr:`Solution.key` 同名，便于按路径定位代码；
* 包内的模块划分建议（照抄 ``minimax_h3/`` 即可）：

  ==================  ==============================================
  模块                职责
  ==================  ==============================================
  ``manifest.py``     模型事实：资产名、模式表、约束常量、示例提示词
  ``builder.py``      把参数摊平成计算图（纯函数，不碰 IO）
  ``solution.py``     :class:`Solution` 实现：声明 + 校验 + 组装作业
  ``templates/``      验证过的参考图（同时作为回归测试的基准）
  ==================  ==============================================

* 以 ``_`` 开头的模块不会被扫描，可用于放私有辅助代码。
"""

from __future__ import annotations

__all__: list[str] = []
