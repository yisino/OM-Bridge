# =============================================================================
# 拓扑档：跨网段 / 经 SSH 反向隧道
# =============================================================================
#
# 适用：OM-Bridge 跑在一台服务器上，ComfyUI 在另一张网（例如你的工作站），
#       两台机器不互通或 ComfyUI 处在鉴权代理之后。
#
# 前置（在 **ComfyUI 所在主机**上执行，保持这个连接不断）：
#     ssh -N -R 8188:127.0.0.1:8188 <user>@<服务器地址>
#
# 验证（在**服务器**上）：
#     curl -s http://127.0.0.1:8188/system_stats | head -c 200
#
# 用法：
#     OMB_ENV_FILE=config/profiles/through-tunnel.env om-bridge doctor
#
# 未列出的配置项一律取内置默认值 —— 默认值本身就是按 H3 的真实部署写的。
# =============================================================================

# 隧道把远端的 8188 映射到本机的 8188，因此这里用回环地址。
OMB_COMFY_SERVER_URL=http://127.0.0.1:8188

# 工作区：产物与物化图的落盘根目录。**强烈建议改成绝对路径** ——
# 相对路径按进程当前目录解析，而 systemd/计划任务的工作目录往往不是你以为的那个。
OMB_WORKSPACE=/home/vincent/MY_WS

# 产物目录留空则用 <工作区>/var/output。
# 经隧道传输的是**小体积的提示词与状态查询**，产物的字节流由后端自身返回，
# 所以输出目录放在本地盘即可，不必与 ComfyUI 同机。
OMB_OUTPUT_DIR=

# 跨网链路抖动的容忍度：探测超时给宽一点，避免误报"不可达"。
OMB_COMFY_CONNECT_TIMEOUT=20

# 长片渲染的等待上限。超时请用 `om-bridge resume` 续等，不要重提。
OMB_TIMEOUT=1800
